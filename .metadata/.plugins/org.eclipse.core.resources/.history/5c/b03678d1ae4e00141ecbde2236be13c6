import java.util.ArrayList;
import java.util.List;
import java.util.Scanner;

public class StrangeNumbers {

	public static void main(String[] args) {
		Scanner sc = new Scanner(System.in);
		int n = sc.nextInt();
		long max=0;
		long[][] time = new long[n][2];
		for (int i1 = 0; i1 < n; i1++) {
			time[i1][0] = sc.nextLong();
			time[i1][1] = sc.nextLong();
			if(time[i1][1]>max){
				max =time[i1][1];
			}
		}
		
		long[] l=stranger(max);

		strangeM(time);
		sc.close();
	}

	private static long[] stranger(long max) {
		List<Long> myList = new ArrayList();
		for (long j = 0; j <= max; j++){
			
		} 
		return null;
	}

	private static void strangeM(long[][] num) {
		 
		for (int i = 0; i < num.length; i++) {
			int flag = 0;
			for (long j = num[i][0]; j <= num[i][1]; j++) {
                long length = (long) Math.log10(j) + 1;
                if(length==1){
                    flag++;
                }else{
                    if((j % length) == 0 ){
                        
                   if (strange(j,length)) {
					flag++;
				}
                }}
                
				}
			System.out.println(flag);
		}

	}

	private static boolean strange(long ls,long length) {
		//long length = (long) Math.log10(ls) + 1;
        long x=ls/ length;
        if((ls % length) == 0){
		if (length<=1) {
			return true;
		} else {			
            long length1 = (long) Math.log10(x) + 1;
			if (strange(x,length1)) {
				return true;
			} else {
				return false;
			}
		}
	}else{
            return false;
        }
    }
}
