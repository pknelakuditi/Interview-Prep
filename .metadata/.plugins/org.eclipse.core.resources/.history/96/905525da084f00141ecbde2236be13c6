import java.util.ArrayList;
import java.util.List;


public class Strange2 {

	public static void main(String[] args) {
		
		
		List<Long> l = strangestM(9);
		for(int i=0;i<l.size();i++){
			l.get(i);
		}
	}
	
	public static List<Long> strangestM(long max) {
		List<Long> l = new ArrayList<Long>();
		for(long i=1;i<10;i++ ){
			if(i<10){
				l.add(i);
			}
		}
		int x=1;
		long t = l.get(x);
		while(t<=max){
			l.addAll(strangest(l.get(x)));
			x++;
		}
		
		return l;
	}
	
	
	public static List<Long> strangest(long x) {
		List<Long> l = new ArrayList<Long>();
		System.out.println("strangest "+x);
	//	List<Long> l = new ArrayList<Long>();
		long i = 2;
		while (i > 1) {
			
			long t = x * i;
			System.out.println("t value "+t);
			if(t<10){
				i++;
				continue;
			}
			long length = (long) Math.log10(t) + 1;
			if (i == length) {
				
				if ((t) % length == 0) {
					//l.add(t);
					l.add(t);
					System.out.println(t+" is strange");
					i++;
				}
			} else if (i > length) {
				System.out.println("before break");
				break;
			} else {
				i++;
				System.out.println(t+" dd");
			}
		}
		return l;
	}

}
