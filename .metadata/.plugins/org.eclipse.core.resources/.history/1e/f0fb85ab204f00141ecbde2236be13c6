import java.util.ArrayList;
import java.util.List;
import java.util.Scanner;

public class StrangeNumbers {

	public static void main(String[] args) {
		// Scanner sc = new Scanner(System.in);
		// int n = sc.nextInt();
		// long max = 0;
		// long[][] time = new long[n][2];
		// for (int i1 = 0; i1 < n; i1++) {
		// time[i1][0] = sc.nextLong();
		// time[i1][1] = sc.nextLong();
		// if (time[i1][1] > max) {
		// max = time[i1][1];
		// }
		// }
		//
		// strangeM(stranger(max + 100000), time);
		// sc.close();

		List<Long> myList = stranger(10000);
		for (int j = 0; j < myList.size(); j++) {
			System.out.println(myList.get(j));
		}
	}

	private static List<Long> stranger(long max) {
		List<Long> myList = new ArrayList<Long>();
		long length;

		for (long j = 1; j <= 10; j++) {
			if (j < 10) {
				myList.add(j);
				// System.out.print(j + ",");
			}
			while(myList.iterator().next()!=null)
			{
				long temp = myList.iterator().next();
				if (temp > max) {
					break;
				}
				myList.addAll(strangest(temp));

			}
		}
		return myList;
	}

	private static void strangeM(List<Long> val, long[][] num) {
		for (int i1 = 0; i1 < num.length; i1++) {
			int flag = 0;
			for (int i = 0; i < val.size(); i++) {

				if (val.get(i) >= num[i1][0] && val.get(i) <= num[i1][1]) {
					flag++;
				}
			}

			System.out.println(flag);
		}
	}

	private static List<Long> strangest(long x) {
		System.out.println("strangest "+x);
		List<Long> l = new ArrayList<Long>();
		long i = 1;
		while (i > 0) {
			long t = x * i;
			long length = (long) Math.log10(t) + 1;
			if (i == length) {
				
				if ((t) % length == 0) {
					l.add(t);
					System.out.println(t+" dd");
					i++;
				}
			} else if (i > length) {
				break;
			} else {
				i++;
				System.out.println(t+" dd");
			}
		}
		return l;
	}

}
